# ===================================================================
# CMEP Backend — Variables de Entorno
# ===================================================================
# Copia este archivo como .env y ajusta los valores segun tu entorno.

# --- Base de datos ---
# DB_URL tiene prioridad. Si no se define, usa SQLite local automaticamente.
# Local (default):  (dejar vacio, usa SQLite)
# Docker MySQL:     mysql+asyncmy://cmep_user:cmep_pass@localhost:3306/cmep_dev
# Produccion AWS:   mysql+asyncmy://cmep_user:PASS@rds-endpoint:3306/cmep_prod
DB_URL=

# Solo se usan si DB_URL esta vacio y se trabaja con docker-compose
DB_HOST=localhost
DB_PORT=3306
DB_NAME=cmep_dev
DB_USER=cmep_user
DB_PASS=cmep_pass

# --- Aplicacion ---
# local | prod
APP_ENV=local
APP_VERSION=0.1.0

# --- Sesiones ---
# IMPORTANTE: cambiar en produccion. Generar con:
#   python -c "import secrets; print(secrets.token_hex(32))"
SESSION_SECRET=dev-secret-change-in-production
SESSION_EXPIRE_HOURS=24

# --- CORS ---
# Origenes permitidos (separados por coma).
# Local:       http://localhost:3000
# Produccion:  https://cmep.tudominio.com
CORS_ORIGINS=http://localhost:3000

# --- Storage ---
# local | s3
FILE_STORAGE=local
UPLOAD_DIR=uploads
# Solo para FILE_STORAGE=s3:
S3_BUCKET=

# --- Cookies (produccion) ---
# Vacio en local. En produccion: .tudominio.com
COOKIE_DOMAIN=
